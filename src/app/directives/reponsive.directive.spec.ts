import { ReponsiveDirective } from './reponsive.directive';

describe('ReponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
